import getData from "../helper/GetData"
import {useState} from "react"

// The alternate view to be used when a user selects a planet
function DetailedView(){
    // Set the data received from the getData Helper
    [setData,data] = useState({});
    return(
    <>

    </>
    )
}